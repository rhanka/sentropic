apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: sentropic
resources:
  - ../../base
  - ingress.yaml
# Images use the base default (:main) today, matching the live cluster. The release
# pipeline pins immutable per-release tags/digests here (BR-55c/d), e.g.:
#   images:
#     - name: rg.fr-par.scw.cloud/nc-reg/sentropic-api
#       newTag: vYYYY.MM.DD.N
# Pins the live OVH storage class on the postgres StatefulSet, mirroring
# overlays/preprod (#474). Without it the prod overlay cannot converge at all.
patches:
  - path: patch-postgres-storageclass.yaml
