{
  "name": "sentropic-vscode-extension",
  "type": "module",
  "displayName": "Sentropic",
  "description": "Sentropic VSCode plugin v1 runtime shell.",
  "version": "0.1.1",
  "publisher": "sentropic",
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onStartupFinished",
    "onCommand:topai.openPanel",
    "onView:topai.chatView"
  ],
  "main": "./dist/extension.cjs",
  "contributes": {
    "commands": [
      {
        "command": "topai.openPanel",
        "title": "TopAI: Open Chat Panel"
      }
    ],
    "viewsContainers": {
      "activitybar": [
        {
          "id": "topai",
          "title": "Sentropic",
          "icon": "topai-icon.svg"
        }
      ]
    },
    "views": {
      "topai": [
        {
          "id": "topai.chatView",
          "type": "webview",
          "name": "Sentropic"
        }
      ]
    },
    "configuration": {
      "title": "Sentropic",
      "properties": {
        "topai.apiBaseUrl": {
          "type": "string",
          "default": "http://localhost:8787/api/v1",
          "description": "Sentropic API base URL used by the VSCode extension runtime."
        },
        "topai.appBaseUrl": {
          "type": "string",
          "default": "http://localhost:5173",
          "description": "Sentropic app base URL used by open-link navigation in VSCode runtime."
        },
        "topai.wsBaseUrl": {
          "type": "string",
          "default": "",
          "description": "Optional websocket base URL used by the VSCode extension runtime."
        },
        "topai.sessionToken": {
          "type": "string",
          "default": "",
          "description": "Session token used as Bearer auth for VSCode extension requests to /api/v1."
        }
      }
    }
  }
}
