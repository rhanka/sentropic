import type { Component } from "svelte";
import type { CitedSourceViewerProps } from "./types.js";

/** The canonical S.6 cited-source frame (see README + `types.ts`). */
declare const CitedSourceViewer: Component<CitedSourceViewerProps>;
export default CitedSourceViewer;
