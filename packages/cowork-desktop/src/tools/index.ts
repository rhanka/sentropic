/**
 * Desktop tools — barrel export. Eyes (`screen_capture`) and hands
 * (`input_action`) as bridge `ToolExecutor`s, plus the consent-gating
 * dispatcher (`runDesktopToolCall`).
 */

export * from './types.js';
export * from './screen-capture.js';
export * from './input-action.js';
export * from './registry.js';
