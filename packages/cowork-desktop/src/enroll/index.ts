/**
 * Device-code enrollment — barrel export. Replaces the proto's pasted token with
 * the RFC 8628-style handshake against `/auth/device/{code,poll}`.
 */

export * from './device-code-client.js';
