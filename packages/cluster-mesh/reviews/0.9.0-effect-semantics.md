---
review-author:
  host: codex
  model: unavailable
  effort: unavailable
target-ref: 9427b448
status: selection-failed
observed-failure: Exact author model and effort metadata are unavailable, so eligible final reviewers cannot be selected without guessing.
---

# 0.9.0 effect-semantics reprise

The remediated target is the committed implementation and test target
`9427b448`. The two authoritative input reviews remain:

- Fable-5.1 contract review: BLOCK at `5ec6c78f`.
- Gemini-3.8 contract review: BLOCK at `5ec6c78f`.

## Remediation evidence

- F1: the three API consumer sites now provide fail-closed instruction, probe,
  and outcome behavior; the API typecheck exits 0.
- F2/F4: invalid and failed outcomes persist `failed` and return 502. The
  `deferred` persist/202 path is contract-level only for the current API consumer:
  its control CHECK cannot store `deferred`, so that path is gated on a future
  owner-approved migration. Terminal-write rejections return a structured 500
  carrying the observed outcome status; only `acted` emits acted evidence.
- F3/wire: `commandRef` is documented opaque and non-executable, the live API
  actuator consumes only a resolved instruction line, and a session body with
  `commandId` alone is rejected with 400. If `commandRef` is also present,
  `commandId` is ignored.
- Gemini Finding 1: LOST reconciliation is fenced to `actuator_unavailable`;
  parked custody failures do not mutate registration state.
- F5/F6: authorization/resolution throws, both custody-epoch mismatches, lost,
  duplicate, target binding, and the authorized OQ12 path have regression tests.
- F7: README, changelog, mount-prefix note, and branch claims reflect 0.9 reality.

## Remaining source gaps

- The external h2a adapter still needs `probeState` and `outcome` before A1 can
  run green; `tests/a1-integration/**` remains excluded from the package gate.
- The API control CHECK in frozen migration 0007 omits `deferred`; storing that
  outcome requires a future owner-gated control migration. No API actuator emits
  `deferred` today.
- OQ1 remains contract-only until a production `CommandInstructionPort` exists.
  The interim API resolver returns null, so live CLI qualification is disabled:
  every control request returns 409 `command_unresolved` without an `effectRef`.

No final consensus verdict is claimed. The owner-gated h-cond review must rerun
against the committed final target before merge.
