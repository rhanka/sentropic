// Canvas layer barrel.
//   types.ts        EditorPort / RendererPort / CanvasCapabilities / Annotation* / CanvasAdapter
//   svelte/         (Lot 2) canvasStore + EditorPane (CodeMirror) + RenderPane (mermaid) + CanvasBoard
export * from './types';
