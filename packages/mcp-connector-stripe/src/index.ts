/**
 * BR-72 read-only benchmark proof — Stripe connector public surface.
 * READ-ONLY ONLY; synthetic fixtures; not the production connector.
 */
export { stripeConnectorAdapter } from './adapter.js';
export { stripeFixtures, getResourceFixture, getToolFixture } from './fixtures.js';
export { stripeManifest } from './manifest.js';
