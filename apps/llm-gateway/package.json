{
  "name": "@sentropic/llm-gateway-host",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Private standalone LLM gateway process: composes @sentropic/llm-gateway through one cluster-mesh registry and namespace module. Not published.",
  "license": "MIT",
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "lint": "eslint \"src/**/*.ts\" \"tests/**/*.ts\"",
    "test": "vitest run tests --environment node"
  },
  "dependencies": {
    "@hono/node-server": "^1.19.15",
    "@sentropic/cluster-mesh": "file:../../packages/cluster-mesh",
    "@sentropic/llm-gateway": "file:../../packages/llm-gateway",
    "@sentropic/llm-mesh": "file:../../packages/llm-mesh",
    "hono": "^4.12.25"
  },
  "devDependencies": {
    "@types/node": "^20.14.2",
    "@typescript-eslint/eslint-plugin": "^8.56.1",
    "@typescript-eslint/parser": "^8.56.1",
    "eslint": "^10.0.2",
    "typescript": "^5.4.5",
    "vitest": "4.1.5"
  }
}
