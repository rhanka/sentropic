# Feature: WP-AGENTS scope items (governance)

## Objective
Populate WP-AGENTS with its owner-stated initial scope (persistent agents + per-role memory, native h2a
CLI loops, track). Conductor governance, no code.

## Branch Scope Boundaries (MANDATORY)
- **Allowed Paths**: `.track/**`, `BRANCH.md`
- **Forbidden Paths**: `Makefile`, `docker-compose*.yml`, `.cursor/rules/**`, `api/**`, `ui/**`, `packages/**`
- **Conditional Paths**: none

## Lots
- [x] Lot 1 — add 3 WP-AGENTS items (live .track off main)
