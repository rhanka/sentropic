---
reviewer-model: gpt-5.6-sol
target-ref: fc9453795
lens: OQ-1/7/9+MVP re-run-3
independent-from-builder: true
---

# Cowork F3 MVP adversarial re-run 3 (cycle 4)

Cycle 3 is still release-blocked. The mismatch, canonical-action, exposure-revoke, migration-text, signed-Notepad, and quiescence patches are present, but the last patch does not provide native cancellation: it waits for an uncancellable native promise and the broker still returns `PAS-FAIT` after a fixed grace even if actuation has not settled. The signed/bounded click trace is closed, but `type` and `scroll` retain a global-focus TOCTOU. Fresh review also found that controller Chat Stop is disconnected from the lease lifecycle and that a post-issue broker/audit/DB exception orphans executable authority.

## Prior findings re-verification

| prior id | status | current evidence and concrete re-attack | minimal fix / residual |
|---|---|---|---|
| SOL-01 — act after revoke/expiry/Stop | **STILL-OPEN** | The runner now starts expiry/server-cancellation watchers after the signed start claim and passes an `AbortSignal` into the tool (`packages/cowork-desktop/src/remote/lease-runner.ts:187-215`). However, `awaitNativeQuiescence` deliberately awaits the complete native promise before it observes that signal (`packages/cowork-desktop/src/capability/windows-provider.ts:39-50`); a 1,024-character `keyboard.type` can therefore keep typing after Stop, expiry, or server cancellation (`windows-provider.ts:165-173`). `stop()` waits for settlement (`lease-runner.ts:123-138`), but the server broker waits only a fixed five-second grace and then returns terminal `PAS-FAIT` regardless of device settlement (`api/src/services/connector-host/cowork.ts:62-75`). Hold native typing beyond that grace: actuation outlives lease expiry and the caller's terminal result. The cycle-3 test itself manually releases the uncancelled provider only after observing abort (`packages/cowork-desktop/tests/remote-lease-runner.spec.ts:242-305`), so it proves deferred reporting inside the runner, not native cancellation or broker quiescence. | Use actually cancellable, bounded Windows primitives (for example, chunked HWND-targeted literal input and a killable capture helper), stop between chunks, and make server finalization wait for an attested settled state rather than a fixed grace. Thread controller Stop into the same cancellation fence (fresh R3-01). |
| SOL-02 — client-self-asserted VM/kiosk gate | **CLOSED** | Client capability flags remain non-authoritative: enrollment requires a pre-existing server provisioning record (`api/src/routes/auth/device.ts:80-97`), only an authenticated `admin_app` can create the exact-Notepad record (`device.ts:123-135`), and issuance rejects production plus locks an active device/presence/provisioning/exposure join (`api/src/services/cowork/device-lease-service.ts:133-205`). Acknowledgement rechecks active exact-Notepad provisioning under the device lock (`device-lease-service.ts:300-327`). The former ordinary-client `isolatedVmTarget:true` self-assertion cannot authorize a lease. | Code-determinable closure. The conductor must still prove in Windows UAT that the provisioned key belongs to the intended isolated VM; this server attestation is not a general-computer-use gate. |
| SOL-03 — same `toolCall.id` aliases distinct invocations | **CLOSED** | Canonical principal/workspace/session/target/capability/action binding is still compared (`api/src/services/cowork/device-lease-service.ts:37-55,89-104,207-217`). Both mismatch updates are now limited to `issued|acknowledged` (`device-lease-service.ts:216-226,251-266`), while issue and start serialize on the same device row (`device-lease-service.ts:181-202,352-363`). If A is `executing`, B's mismatched issue returns `not_issuable` and cannot mutate A; if start and mismatch race, the device lock orders them. | Code-determinable closure; the exact executing-owner predicate is structurally exclusive. |
| SOL-04 — missing device/workspace/capability exposure | **CLOSED** | Exact exposure is still required during discovery/account resolution and final mount invocation (`api/src/services/connector-host/cowork.ts:120-148,160-170`), target selection (`api/src/services/cowork/target-selection.ts:32-42`), and locked issuance (`api/src/services/cowork/device-lease-service.ts:184-205`). No cross-workspace implicit grant or mutable mount cache was reintroduced. | Code-determinable closure of the original implicit-exposure bug. F-01's live revocation race is separately `PENDING-RUNTIME`. |
| SOL-05 — reusable capture consent | **CLOSED** | Both remote capabilities delete legacy persisted allows, require a foreground decision, and mint a receipt bound to tool/lease/action digest (`packages/cowork-desktop/src/consent/manager.ts:72-104`). Matching consumption deletes the receipt synchronously (`consent/manager.ts:106-117`; `packages/cowork-desktop/src/tools/registry.ts:55-69`). A forged object, another lease/action, or replay cannot consume the map entry. | Code-determinable closure; Windows prompt presentation remains UAT. |
| SOL-06 — control/submission characters through `type` | **CLOSED** | The server and device exact schemas reject extra action keys, chords/key actions, unsafe integers, Unicode control/format/surrogate/line/paragraph characters, empty text, and text over 1,024 code points (`api/src/services/cowork/input-action-schema.ts:1-59`; `packages/cowork-desktop/src/tools/input-action-schema.ts:5-32`). Remote dispatch reaches only click/type/scroll, and the Windows type path exposes no Enter/key/clipboard/IME fallback (`packages/cowork-desktop/src/tools/input-action.ts:47-79`; `packages/cowork-desktop/src/capability/windows-provider.ts:165-173`). | Code-determinable allowlist closure. N-01 still allows the safe literal to hit the wrong focused HWND. |
| SOL-07 — capture pixels dropped while returning FAIT | **CLOSED** | The runner posts the provider result (`packages/cowork-desktop/src/remote/lease-runner.ts:206-215`); the server accepts capture `FAIT` only with bounded non-empty PNG data, primary-screen metadata, device signature, and `executing -> consumed`, then persists it (`api/src/services/cowork/device-lease-service.ts:59-87,393-435`). The broker returns only the persisted result (`device-lease-service.ts:440-454`; `api/src/services/connector-host/cowork.ts:100-105`). Missing pixels cannot become `FAIT`. | Code-determinable closure; real primary-display pixels/dimensions remain Windows UAT. |
| N-01 — no DPAPI identity / measured runtime kiosk guard | **STILL-OPEN** | The rerun-2 copied/unsigned executable and resize/out-of-bounds click traces are stopped: the guard requires canonical `%Windows%\\System32\\notepad.exe`, a valid Microsoft signer, stable HWND/PID/client bounds (`packages/cowork-desktop/src/capability/foreground-surface.ts:30-79`), and click uses an in-bounds HWND-targeted helper that rechecks foreground HWND/PID/bounds (`foreground-surface.ts:81-109,154-201`; `packages/cowork-desktop/src/capability/windows-provider.ts:152-163`). But type and scroll still perform a PowerShell foreground recheck, return to JavaScript, then call global `nut.keyboard.type`/mouse scrolling (`windows-provider.ts:165-188`). Switch focus after the recheck returns but before/during the native call: text or scroll reaches the new window; the post-call recheck can only turn the result into `PAS-FAIT` after unauthorized input occurred. | Add HWND-targeted, guard-and-act Windows primitives for literal type and scroll too; the same native helper must validate exact HWND/PID/signed executable/client bounds immediately around each bounded input chunk. DPAPI restart/copy and the native helper remain Windows-hardware UAT. |
| N-02 — narrowed/malformed capture broadens to full-screen FAIT | **CLOSED** | Issuance and execution still accept only `{}` or `{screen:0}` with no extra key (`api/src/services/cowork/screen-capture-action-schema.ts:1-12`; `packages/cowork-desktop/src/tools/screen-capture.ts:29-49`), the provider refuses a region/non-primary display (`packages/cowork-desktop/src/capability/windows-provider.ts:132-143`), and result acceptance requires `screen:0` (`api/src/services/cowork/device-lease-service.ts:59-69`). | Code-determinable closure; display selection remains Windows UAT. |
| N-03 — no product path to create an exposure grant | **CLOSED** | `/auth/device/cowork-exposure` still validates a non-empty unique capability subset and requires a live `admin_app` (`api/src/routes/auth/device.ts:66-72,138-160`). The transaction requires active device ownership plus workspace admin and records `grantedBy` (`api/src/services/cowork/provisioning.ts:71-89,114-118`; `api/src/db/schema.ts:249-259`). This is a shipped authenticated path, not test-only SQL. | Code-determinable path closure. Its real API/Postgres execution could not be rerun in this checkout. |
| F-01 — exposure revoke leaves issued authority executable | **PENDING-RUNTIME** | The static transaction is aligned: it locks the device and exact grant, revokes matching `issued|acknowledged` leases by durable workspace/capability binding, then deletes the grant (`api/src/services/cowork/provisioning.ts:78-112`). Ack and start recheck the exact grant under the same device fence (`api/src/services/cowork/device-lease-service.ts:300-327,352-368`). Thus held consent loses, and post-ack start/revoke has one authorization winner. The real PostgreSQL/API race suite did not run here, so raw SQL/lock behavior is not claimed closed from mocks. | Run `cowork-exposure.spec.ts` on a migrated Postgres/API stack, including held-consent and post-ack races, before changing this to `CLOSED`. |
| F-02 — migration rejects `executing` | **PENDING-RUNTIME** | Migration 0041 now admits `executing` in both the status check and partial idempotency index (`api/drizzle/0041_cowork_device_identity_lease.sql:29-47`), and Drizzle declares the same partial index (`api/src/db/schema.ts:261-283`). This fixes fresh-database text, but editing an already-applied migration cannot change an existing database and no real migration/start trace ran here. | Apply from a fresh migration baseline (or add the required forward migration for any already-initialized target), inspect the live constraint/index, and run issue -> ack -> start -> complete on Postgres. |
| F-03 — malformed action normalized and returns FAIT | **CLOSED** | Server and device now share the exact discriminated contract; explicit invalid buttons, unknown keys, fractional/missing coordinates, zero/missing scroll axes, and unsafe type text are rejected, while omitted click button alone canonicalizes to left (`api/src/services/cowork/input-action-schema.ts:29-59`; `packages/cowork-desktop/src/tools/input-action-schema.ts:10-32`). Issuance stores/hashes the canonical action (`api/src/services/cowork/device-lease-service.ts:140-157,229-246`), and `FAIT` must carry its exact canonical action digest (`device-lease-service.ts:71-87`). The prior `"right " -> left` trace fails before signing. | Code-determinable closure. |

## New findings

| severity | file:line | exploit | minimal fix |
|---|---|---|---|
| **critical** | `api/src/routes/api/chat.ts:59-92`; `api/src/services/chat-service.ts:3842-3855,3877-3888`; `api/src/services/catalog/sources/standalone-tool-source.ts:54-64`; `api/src/services/skills/foundation-executor.ts:583-588`; `api/src/services/connector-host/cowork.ts:93-105`; `packages/cowork-desktop/src/cli/run.ts:151-159` | **R3-01 — controller Chat Stop never revokes the active Cowork lease.** (1) Start a Cowork tool and hold its target consent prompt, or let it reach `executing`. (2) Press Stop in controller A. The route only cancels/finalizes the chat job. Although chat runtime has an `AbortSignal`, the standalone-tool context omits it and the foundation executor calls Cowork without it. (3) The broker continues its independent 30-second wait; target `RemoteLeaseRunner.stop()` is wired only to target-process SIGINT/SIGTERM. (4) Approve before timeout: ack/start and input still occur after controller Stop. This violates the explicit Stop/no-later-execution condition. | Add `AbortSignal` to the catalog invocation context and per-call broker. Immediately fence/revoke the issued lease on abort and do not finalize the stopped chat job until an executing device has signed/committed quiescence. Add a controller `/stop` -> held-consent and post-start provider race. |
| **critical** | `api/src/services/connector-host/cowork.ts:93-105`; `packages/chat-core/src/runtime-tool-dispatch.ts:929-989`; `api/tests/unit/cowork-broker.test.ts:7-72` | **R3-02 — a post-issue exception escapes without revocation.** (1) Let `broker.issue` commit a lease. (2) Inject a transient rejection from the `lease_issued` audit, `broker.wait`, `readLeaseOutcome`, or timeout revocation. (3) `createCoworkInvocationBroker` has no post-issue `try/finally`; the exception reaches the generic tool catch, which reports `status:error`. (4) The durable lease remains `issued`, `acknowledged`, or `executing`; after the transient fault clears, the device can still consent/ack/start/act while the caller already received a terminal error. The broker-closure tests cover only resolved outcomes and never fault this seam. | Once issuance succeeds, wrap every audit/wait/result path in an exception-safe lifecycle guard. On any error/abort, atomically revoke pre-start authority or request executing cancellation, await bounded signed quiescence, then return `PAS-FAIT`. Add fault injection immediately after issue, during wait, during audit, and during cancellation. |

## Fresh attack results and residual classification

- **OQ-1/C1:** The shared API mount retains ports only and copies trusted principal/workspace/session/target/toolCall/action into a fresh closure (`api/src/services/connector-host/cowork.ts:151-172`). The session selection singleton is keyed by user/workspace/session (`api/src/services/cowork/target-selection.ts:13-24,45-52`), and consent receipts/active executions are keyed per lease. Same/different-ID result aliasing is stopped by the durable invocation binding. R3-01/R3-02 nevertheless refute complete lifecycle ownership under Stop and faults.
- **OQ-7/C4:** The server signature preimage remains exactly `{leaseId, capability, targetDeviceId, nonce, expiry}` (`api/src/services/cowork/lease-envelope.ts:6-38`). The device rejects missing/wrong `kid`, absent/forged MAC, expiry, or any signed-field change before consent/ack/action (`packages/cowork-desktop/src/remote/lease-runner.ts:140-176,230-237`). Device-signed ack and start are independently verified, and conditional `issued -> acknowledged -> executing -> terminal` updates reject replay/double completion (`api/src/services/cowork/device-lease-service.ts:275-369,393-435`). No C4 forgery/replay blocker was found.
- **OQ-9/C5:** Production minting is denied and active server provisioning/exposure is rechecked. Device/account deletion locks the device, revokes `issued|acknowledged`, refuses a cascade while `executing`, then deletes only when no executable row remains (`api/src/services/cowork/device-registry.ts:75-99`; `api/src/routes/api/me.ts:308-325`; `api/src/routes/api/admin.ts:366-383`). No lease survives a successful code path, but Postgres runtime proof is pending and SOL-01/R3-01 refute Stop/timeout containment.
- **OQ-5 and consent:** Only exact click/scroll/type schemas are reachable; Enter/chords/control text, malformed actions, receipt replay/forgery, receipt cross-lease/action reuse, and durable remote allows fail closed. N-01 is the remaining actuation-target violation, not an allowlist expansion.
- **OQ-8/I5:** Normal denial, malformed, offline, expiry, replay, mismatch, and timeout branches do not create `FAIT`; only a signed, validated, persisted `consumed` result does. SOL-01 and R3-01/R3-02 remain release blockers because external action can outlive a terminal `PAS-FAIT`, Stop, or error even though the code does not label that action `FAIT`.
- **Verification at `fc9453795`:** `make typecheck-api ENV=test-cowork-connector`, desktop typecheck, and connector-host typecheck passed. Desktop tests passed 15 files / 84 tests; connector-host passed 1 file / 11 tests. The scoped lease, exposure, and broker API commands each failed before Vitest with `service "api" is not running`; F-01/F-02 therefore remain `PENDING-RUNTIME`. Windows DPAPI, signed executable probing, HWND-targeted click delivery, global-focus closure for type/scroll, native cancellation, capture pixels, and primary-display behavior require Windows-hardware UAT after the code blockers are fixed.

**VERDICT: RELEASE-BLOCK — SOL-01 and N-01 remain open, R3-01 and R3-02 are fresh critical blockers, and F-01/F-02 still require a real Postgres/API run. This is not CLEAR-FOR-DEMO.**
