---
reviewer-model: gpt-5.6-sol
target-ref: 74e81f46c
lens: OQ-1/7/9+MVP re-run
independent-from-builder: true
---

# Cowork F3 MVP adversarial re-run

`SOL-01` through `SOL-07` below identify the seven rows of the prior Codex-Sol dossier in their original order. I re-read the remediation, its tests, and the current call paths rather than relying on the Terra closure summary. Six original exploits are closed, but the original revoke race remains reachable after acknowledgement. Fresh attacks also found a runtime kiosk-guard bypass, a malformed capture that returns `FAIT`, and no shipped way to create the new mandatory exposure grant.

## Prior findings re-verification

| prior id | severity | prior finding | status | current evidence and re-attack | minimal fix if open |
|---|---:|---|---|---|---|
| SOL-01 | critical | Act after revoke/expiry/Stop | **STILL-OPEN** | Consent now precedes acknowledgement and local expiry/cancellation is checked (`packages/cowork-desktop/src/remote/lease-runner.ts:121-137`), closing the held-prompt exploit. It does not close the post-ack race: after the successful `await acknowledge`, revoke/delete can atomically change `acknowledged -> revoked` (`api/src/services/cowork/device-lease-service.ts:337-343`; `api/src/services/cowork/device-registry.ts:83-94`), while the runner checks only local time/cancellation and then calls the provider (`lease-runner.ts:134-143`). The added tests substitute a failed ack while consent is held (`packages/cowork-desktop/tests/remote-lease-runner.spec.ts:76-121`); none races revoke after a 200 ack. | Add an execution fence/state in which revoke/delete and the device's final start claim have one winner. If start wins, revoke/delete must not report no-action or delete until a signed terminal result; if revoke wins, the device must receive/acknowledge cancellation before provider entry. Serialize local Stop/new delivery/provider entry behind a stopped latch and abortable execution lock. |
| SOL-02 | critical | Client-self-asserted VM/kiosk gate | **CLOSED** | Client claims are now ignored (`api/src/routes/auth/device.ts:36-45`); only an `admin_app` can create the exact-Notepad key record (`device.ts:107-124`). Enrollment and activation recheck it (`device.ts:68-85`; `api/src/services/cowork/device-identity.ts:117-126`), issuance joins the active record under the device lock (`device-lease-service.ts:149-170`), and acknowledgement rechecks it (`device-lease-service.ts:261-275`). This stops the original unauthenticated `isolatedVmTarget:true,kioskSurface:"powershell"` exploit. The record's failure to prove the current machine/surface is a distinct fresh blocker, N-01. | — |
| SOL-03 | high | Same `toolCall.id` aliases distinct invocations | **CLOSED** | The broker now carries workspace/session (`api/src/services/connector-host/cowork.ts:53-59,153-164`) and persists principal, workspace, session, target, capability, and action hash (`api/src/services/cowork/device-lease-service.ts:31-49,118-125,193-206`). An idempotency hit is returned only on complete binding equality; a mismatch revokes and denies, including the insert race (`device-lease-service.ts:175-190,215-230`). Identical retries may converge; distinct invocations cannot inherit their result. | — |
| SOL-04 | high | No connector-account-workspace exposure | **CLOSED** | A durable `(device, workspace, capability)` grant now exists (`api/src/db/schema.ts:249-259`) and is required during discovery/account resolution/invoke (`api/src/services/connector-host/cowork.ts:113-140,153-164`), target selection (`api/src/services/cowork/target-selection.ts:32-42`), direct issuance (`api/src/routes/api/chrome-extension.ts:178-209`), and locked lease eligibility (`api/src/services/cowork/device-lease-service.ts:152-170`). The original workspace-B implicit exposure fails closed. The absence of a product grant-creation path is N-03. | — |
| SOL-05 | high | Reusable capture consent | **CLOSED** | Remote persisted allows are deleted, not honored, and every lease prompts (`packages/cowork-desktop/src/consent/manager.ts:67-104`). The in-memory receipt binds tool, lease, and action digest and is atomically removed on its sole consumption (`consent/manager.ts:106-117`; `packages/cowork-desktop/src/tools/registry.ts:55-70`). Replaying or copying it to another action/lease fails before the executor. | — |
| SOL-06 | high | Control/submission characters pass through `type` | **CLOSED** | Issuance accepts only bounded text without `Cc`, `Cf`, `Cs`, `Zl`, or `Zp` (`api/src/services/cowork/input-action-schema.ts:1-34`). The device repeats the same check (`packages/cowork-desktop/src/tools/literal-text.ts:1-29`), the executor dispatches only click/type/scroll (`packages/cowork-desktop/src/tools/input-action.ts:56-92`), and the Windows type path checks again before its sole `keyboard.type` call (`packages/cowork-desktop/src/capability/windows-provider.ts:146-153`). Enter, Tab, escape/control ranges, paste/IME actions, chords, and synthetic `key` actions have no reachable remote dispatch. | — |
| SOL-07 | high | `screen_capture` drops pixels but returns `FAIT` | **CLOSED** | The runner now sends the executor result under a device signature that includes its canonical digest (`packages/cowork-desktop/src/remote/lease-runner.ts:138-143,200-205`). The server requires a non-empty image of at most 4 MiB, verifies the digest-bound signature, and persists it only on the atomic acknowledged-to-consumed transition (`api/src/services/cowork/device-lease-service.ts:53-70,278-313`). The broker reads and returns that accepted result (`device-lease-service.ts:316-334`; `api/src/services/connector-host/cowork.ts:93-98`). Missing/malformed capture output cannot produce `FAIT`. | — |

## New findings

| severity | OQ | file:line | exploit | fix |
|---|---|---|---|---|
| **critical** | OQ-9 / C5; OQ-5; consent | `api/src/services/cowork/provisioning.ts:17-40`; `packages/cowork-desktop/src/enroll/device-identity.ts:8-16`; `packages/cowork-desktop/src/storage/file-store.ts:31-49,101-107`; `packages/cowork-desktop/src/cli/run.ts:34-39`; `packages/cowork-desktop/src/capability/windows-provider.ts:107-153` | **N-01 — the new key record is not a runtime kiosk guard.** (1) Provision a legitimate isolated VM key, then copy its exportable `device-identity.json` to an ordinary Windows host, or merely put another process/system dialog in the foreground on the provisioned VM. (2) The static public-key row still says `notepad`; no server or device code measures the machine, process, HWND, executable, isolation, or foreground surface. (3) The prompt displays only the model's JSON—not the current window, preview, expiry, or reversibility. (4) Absolute clicks and `keyboard.type` operate globally/current-focus, so the valid lease can actuate PowerShell, a credential dialog, or another application while every “Notepad attestation” check passes. This is execution against a non-attested surface and a missed sensitive confirmation. | For the contained demo, bind provisioning to a non-exportable machine identity and require a device-side final guard on the exact allowlisted executable/window (including recheck after consent and native-module awaits). Display the measured window plus capture/coordinates in the foreground prompt. Refuse on focus/process drift; a database label alone cannot authorize input. |
| **high** | OQ-8 / I5; consent | `api/src/services/cowork/device-lease-service.ts:107-113`; `packages/cowork-desktop/src/tools/screen-capture.ts:41-50,61-76`; `packages/cowork-desktop/src/capability/windows-provider.ts:107-121`; `api/src/services/cowork/device-lease-service.ts:53-59` | **N-02 — malformed or narrowed capture becomes a broader successful capture.** Submit `screen_capture {screen:-3,region:{x:"bad"}}`, or request a valid narrow region. The server validates only `input_action`; the device silently normalizes malformed fields to `undefined`, and the Windows provider explicitly ignores every region and captures the full screen. The resulting image satisfies the server's image-only validator, so the lease is consumed and the caller receives `FAIT` for a different, privacy-broader observation. The test at `packages/cowork-desktop/tests/tools.spec.ts:54-57` currently codifies this normalization. | Give `screen_capture` a strict server-and-device action schema. Until crop/display selection is implemented and verified, reject `region` and non-default `screen`; otherwise crop before result signing and validate action-bound dimensions/metadata before consuming the lease. Malformed or mismatched capture must be `PAS-FAIT`. |
| **high** | OQ-1 / C1; MVP operability | `api/src/services/cowork/provisioning.ts:56-64`; `api/src/services/cowork/target-selection.ts:32-42`; `api/src/routes/auth/device.ts:107-124`; `api/src/routes/api/chrome-extension.ts:247-260` | **N-03 — the mandatory exposure grant has no shipped creation path.** On a fresh deployment, an admin can provision and a user can enroll a device, but no route or production service calls `grantCoworkWorkspaceExposure`; repository call-site search finds only tests. Selection therefore denies at `hasExposure`, discovery returns no capabilities, and the real demo can never issue a lease without manual SQL or importing a test/service helper. The exposure fix is fail-closed but operationally unreachable. | Add an authenticated conductor/admin grant/revoke route or atomic provisioning workflow that verifies device ownership, workspace access, exact capability subset, and audit provenance. Exercise provision -> enroll -> grant -> select -> invoke through public routes in the scoped API suite. |

## Fresh-leg attack summary and traces

- **OQ-7/C4 held:** wrong/absent `kid` has no key; malformed/absent MAC fails; the device verifies before consent/ack/action (`lease-runner.ts:108-159`); and changing any of lease id, capability, device id, nonce, or ISO expiry changes the signed preimage (`api/src/services/cowork/lease-envelope.ts:16-38`). The independent device ack is signed over lease id + nonce and the database accepts only one live `issued -> acknowledged` transition (`device-lease-service.ts:239-275`). Replay of consumed/expired/revoked rows cannot acknowledge again.
- **OQ-1 shared-state attack held except where noted:** the durable mount stores ports only and creates a fresh closure per invocation (`api/src/services/connector-host/cowork.ts:144-165`); selection singleton state is keyed by user/workspace/session (`api/src/services/cowork/target-selection.ts:13-24,45-52`); all device/exposure queries bind the authenticated user. Complete invocation binding closes same-id cross-session/workspace/capability/action aliasing.
- **Atomic database terminalization held, end-to-end revocation did not:** conditional updates give consume/expire/revoke one database winner (`device-lease-service.ts:303-343`), and device/account deletion updates in-flight leases before cascade (`api/src/services/cowork/device-registry.ts:75-95`; `api/src/routes/api/me.ts:306-315`). SOL-01 shows why the already-delivered device authority can still act after that database winner.
- **Test traces:** `make test-cowork-desktop ENV=test-cowork-connector` passed 13 files / 72 tests; `make test-connector-host ENV=test-cowork-connector` passed 1 file / 11 tests. The scoped API attempt could not run: after `make up-api-test ENV=test-cowork-connector`, `make test-api-endpoints SCOPE=tests/api/cowork-device-leases.spec.ts API_TEST_WORKERS=1 ENV=test-cowork-connector` failed with `service "api" is not running`, matching the branch's recorded unavailable-stack limitation. No API runtime pass is claimed.

**VERDICT: RELEASE-BLOCK**
